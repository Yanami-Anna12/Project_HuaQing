# **考试_python基础**

## **一、填空题** **每题 2 分（共 5 题）→** **10 分**

    1.     Python 中用于接收用户输入的函数是_____input()_____。

    2.     range(2, 8, 2) 生成的序列是___2,4,6_______。（用逗号分隔，不加空格）

    3.     字符串 "Python"[::-1] 的结果是_____nohtyP_____。

    4.     列表 [1, 2, 3] * 2 的结果是____[1,2,3,1,2,3]______。

    5.     执行 print("A" in "Apple") 的输出是 ______True____。

## **二、判断题** **每题 2 分（共 8 题）→** **16 分**

    6.     pass 语句没有任何作用，可以删除而不影响程序逻辑。（F  ）

    7.     break 可以跳出所有嵌套循环。（ F ）

    8.     字符串可以通过下标修改其中的字符。（ F ）

    9.     list.append(x) 会将 x 插入到列表开头。（ F ）

    10.     range(5) 生成的是 0 到 5（包含 5）的整数序列。（ F ）

    11.     continue 会终止整个循环。（ F ）

    12.     sorted([3,1,2]) 会直接修改原列表。（ F ）

    13.     eval("3 + 5") 的结果是字符串 "8"。（ T ）

## **三、选择题** **每题 3 分（共** **9** **题）→** **27** **分**

    14.     下列哪项不是合法的变量名？B

A. _name

B. 2var

C. userName

    D.     my_var

    15.     执行以下代码后，x 的值是？A

```
x = 10 if 5 > 3 else 20
```

A. 10

B. 20

C. True

    D.     报错

    16.     下列哪个表达式的结果为 True？C

A. bool("")

B. bool(0)

C. bool("False")

    D.     bool(None)

    17.     以下代码输出什么？B

```
for i in range(3):
    if i == 1:
        continue
    print(i)
```

A. 0 1 2

B. 0 2

C. 1

    D.     0 1

    18.     下列哪行代码能正确创建一个空字典？B

A. d = []

B. d = {}

C. d = ()

    D.     d = set()

    19.     已知 s = "hello"，则 s[1:4] 的结果是？A

A. "ell"

B. "ello"

C. "llo"

D. "hel"

    20.     下列哪项不能用于遍历字符串？D ????????

A. for char in "abc":

B. for i in range(len(s)):

C. while i < len(s):

D. for char in enumerate(s)

    21.     执行以下代码后，lst 的值是？B

```
lst = [1, 2, 3]
lst.insert(1, 'a')
```

A. [1, 'a', 2, 3]

B. ['a', 1, 2, 3]

C. [1, 2, 'a', 3]

    D.     [1, 2, 3, 'a']

    22.     下列哪项是正确的 JSON 字符串？D

A. "{'name': 'Tom'}"

B. '{"name": "Tom"}'

C. {"name": "Tom"}

    D.     "name=Tom"

## **四、填空题** **每空 2 分（共** **8** **空）→** **16** **分**

    23.     补全代码：求 1 到 100 中能被 3 整除的数的个数。

```Python
count = 0
i = 1
while i <= 100:
    if __i % 3 == 0______:      # (1)
        count += 1
    ___i++_____          # (2) 迭代条件
print(count)
```

    24.     补全代码：使用列表推导式，从 nums = [1, 4, 6, 9, 10] 中提取所有奇数。

```Python
odds = [__n____ for ___n___ in nums if __n % 2 == 1____]  # (3)(4)(5)
print(odds)
```

    25.     补全代码：遍历字典 d = {'a': 1, 'b': 2}，打印每个键值对。

```Python
for __key____, ___value___ in d.items():  # (6)(7)
    print(f"{key} -> {value}")
```

    26.     补全代码：翻转字符串 s = "world" 并输出。

```Python
s = "world"
print(__s[::-1]______)  # (8)
```

## **五、编程题** **（** **8 + 8 + 8 + 8****）→** **32** **分**

27. 已知列表 scores = [85, 92, 78, 96, 88]，要求：（8分）

a) 使用 max() 和 min() 找出最高分和最低分

b) 计算平均分（保留一位小数）

c) 使用 sorted() 生成从高到低排序的新列表（不修改原列表）

d) 输出格式如下：text编辑

```
最高分：96
最低分：78
平均分：87.8
排名：[96, 92, 88, 85, 78]
```

    28.     给定字符串 text = "  Hello, Python!  "，完成并依次输出：（8分）

a) 去除首尾空格

b) 转为大写

c) 统计字母 'o' 出现的次数

d) 判断是否以 "Hel" 开头

    29.     已知列表 scores = [85, 92, 78, 96, 88]，要求：(8分)

a) 使用 max() 和 min() 找出最高分和最低分

b) 计算平均分（保留一位小数）

c) 使用 sorted() 生成从高到低排序的新列表（不修改原列表）

d) 输出格式如下：

```
最高分：96
最低分：78
平均分：87.8
排名：[96, 92, 88, 85, 78]
```

    30.     创建一个字典 student，包含键：'name'、'age'、'courses'（课程列表）。(8分)

    ● 初始化：name='张伟', age=20, courses=['数学', '英语']

    ● 添加新课程 '物理'

    ● 删除 '英语' 课程

    ● 最后打印整个字典
